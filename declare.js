const speaker = {Brand: 'Yobetti', Price: 400, color: 'Balck and Silver'}

const charger = new Object() //ei bhabew amra object declare korte pari and ek ba ekadim value add korte pari..
console.log(charger);

const mouse = Object.create({}); //ei bhabew amra object declare kortepari..
console.log(mouse);


const str = "Hello";
str[0] = "h";
console.log(str);